PORT=

DB_HOST=
DB_USER=
DB_PASS=
DB_PORT=
DB_NAME_DB=

JWT_SECRET=
JWT_EXPIRES_IN=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# SMTP Google (usar contraseña de aplicación, no la contraseña de la cuenta)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_FROM_NAME="Buy & Sell"

# Solo desarrollo: permite probar Swagger sin JWT (nunca en producción)
AUTH_BYPASS=false
NODE_ENV=develop

# Origen permitido para Socket.io (ej. http://localhost:4200)
CORS_ORIGIN=http://localhost:4200